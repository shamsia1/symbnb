{% extends 'admin/base.html.twig' %}

{% block title %}Administration des announces{% endblock %}

{% block body %}
<div class="container-fluid">
    <h1 class="my-5">Gestion des announces</h1>
    <table class="table rable-hover">
        <thead>
            <tr>
                <td>Id</td>
                <td>Titre</td>
                <td>Auteur</td>
                <td class="text-center">Réservations</td>
                <td class="text-center">Note</td>
                <td></td>
            </tr>
        </thead>
        <tbody>
            {% for ad in ads %}
            <tr>
                <td>{{ad.id}}</td>
                <td>{{ad.title}} </td>
                <td><img src="{{ad.author.picture}}" alt="Avatar de {{ad.author.fullName}}" class="avatar avatar-micro mr-2">
                    {{ad.author.fullName}}
                </td>
                <td class="text-center">
                    <span class="badge badge-primary rounded"> {{ad.bookings|length}} </span>
                </td>
                <td class="text-center">
                    <span class="badge badge-primary rounded"> {{ad.avgRatings| number_format(2,',', '')}}</span>

                </td>
                <td>
                    <a href="#" class="btn btn-primary"><i class="fas fa-edit"></i></a>
                    <a href="#" class="btn btn-danger"><i class="fas fa-trash"></i></a>
                </td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}
